APP_NAME=avatar-api
APP_ENV=development
LOG_LEVEL=info
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=avatar
DB_USER=avatar
DB_PASSWORD=avatar
DB_MODE=docker
# For local DB mode only (optional command), example:
# LOCAL_PG_CHECK_CMD=psql -h 127.0.0.1 -p 5432 -U avatar -d avatar -c "select 1"
