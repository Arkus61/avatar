# Contracts Package

Shared API contract artifacts.

- `openapi/` stores source OpenAPI specs exported from backend.
- `generated/` stores generated clients/types consumed by web app.

This package is the contract synchronization boundary between `apps/api` and `apps/web`.
